#!/usr/bin/env zsh

# alias communs
alias bat="batcat"
alias df="duf"
alias fd="fd"
alias free="free -h"
alias ls="eza --color=always --group-directories-first"
alias l='ls -lhg'
alias ll="l -a"
alias la="l -av"
alias dir="l"

# pretty print PATH
alias ppp='echo $PATH | tr ":" "\n"'
