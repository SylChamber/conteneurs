#!/usr/bin/env zsh

# alias communs
alias bat="batcat"
alias df="duf"
alias fd="fd"
alias free="free -h"
alias ls="ls --color=always --group-directories-first"
alias l='ls -lh'
alias ll="l -a"
alias la="l -av"
alias dir="l"
alias mc=micro

# pretty print PATH
alias ppp='echo $PATH | tr ":" "\n"'
